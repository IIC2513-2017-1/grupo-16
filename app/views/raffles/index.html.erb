<p id="notice"><%= notice %></p>
<!--
<h1>Listing Raffles</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @raffles.each do |raffle| %>
      <tr>
        <td><%= link_to 'Show', raffle %></td>
        <td><%= link_to 'Edit', edit_raffle_path(raffle) %></td>
        <td><%= link_to 'Destroy', raffle, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
-->
<div class="raffles">
  <h1>Rifas: <%= @raffles.size %></h1>
  <ol>
    <% @raffles.each do |raffle| %>
      <%= render 'raffles/preview', raffle: raffle %>
    <% end %>
  </ol>
  <%= link_to 'Nueva Rifa', new_raffle_path %>
</div>
