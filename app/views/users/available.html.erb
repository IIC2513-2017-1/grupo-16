<ol>
  <% Raffle.where("NOT user_id = ?", @user.id).each do |r| %>
    <% if not Participate.where("raffle_id = ? AND user_id = ?", r.id, @user.id).exists? %>
      <%= render 'raffles/preview', raffle: r %>
    <% end %>
  <% end %>
</ol>